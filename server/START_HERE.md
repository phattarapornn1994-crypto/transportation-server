# 🚀 วิธีเริ่มต้นใช้งานระบบจัดการขนส่ง

## ขั้นตอนการติดตั้งและรัน

### 1. ติดตั้ง Dependencies

เปิด Terminal/PowerShell 2 หน้าต่าง:

**Terminal 1 - Backend:**
```bash
cd server
npm install
```

**Terminal 2 - Frontend:**
```bash
cd client
npm install
```

### 2. รันแอปพลิเคชัน

**Terminal 1 - รัน Backend Server:**
```bash
cd server
npm run dev
```
Server จะรันที่: **http://localhost:5000**

**Terminal 2 - รัน Frontend:**
```bash
cd client
npm run dev
```
Frontend จะรันที่: **http://localhost:3000**

### 3. เปิดเบราว์เซอร์

เปิดเบราว์เซอร์และไปที่: **http://localhost:3000**

## 📋 ฟีเจอร์ที่พร้อมใช้งาน

✅ **จัดการข้อมูลลูกค้า** - เพิ่ม/แก้ไข/ลบ/ค้นหาลูกค้า
✅ **จัดการข้อมูลรถ** - จัดการรถขนส่งพร้อมความจุและต้นทุน
✅ **สร้างแผนการขนส่ง** - ฟอร์มแบบ Step-by-Step
✅ **วิเคราะห์เส้นทาง** - Clustering + Route Optimization
✅ **ผลลัพธ์แผน** - แสดงสรุปและรายละเอียดเส้นทาง
✅ **รายงาน** - ส่งออก PDF/Excel พร้อมกราฟ

## ⚠️ หมายเหตุ

- ฐานข้อมูล SQLite จะถูกสร้างอัตโนมัติเมื่อรัน server ครั้งแรก
- ไฟล์ฐานข้อมูล: `server/transportation.db`
- หากพบปัญหา ให้ตรวจสอบว่า Node.js และ npm ติดตั้งแล้ว

## 🎯 เริ่มใช้งาน

1. ไปที่หน้า "จัดการลูกค้า" เพื่อเพิ่มข้อมูลลูกค้า
2. ไปที่หน้า "จัดการรถขนส่ง" เพื่อเพิ่มข้อมูลรถ
3. ไปที่หน้า "สร้างแผนการขนส่ง" เพื่อสร้างแผนใหม่
4. ระบบจะวิเคราะห์และสร้างเส้นทางอัตโนมัติ
5. ดูผลลัพธ์และส่งออกรายงานได้

